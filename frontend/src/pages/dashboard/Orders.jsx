/**
 * Run 1: Orders — placeholder until Run 10 (restaurants only)
 */

import PlaceholderPage from '../../components/layout/PlaceholderPage'

const Orders = () => (
  <PlaceholderPage
    title="Orders"
    icon="fa-bag-shopping"
    description="Order queue, accept/reject, active orders board, Kitchen Display, and Uber Direct integration."
    requiredTier="Growth"
  />
)

export default Orders
